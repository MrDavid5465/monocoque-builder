[ABOUT]
NAME = TyPiQL Puppet
AUTHOR = TyPiQL
VERSION = 1.0.0
DESCRIPTION = Drives this car's position, orientation and lights from a stream of frames sent by TyPiQL, for a rendered view mirroring another session.

[CORE]
; Loads with the game and runs until it closes, same as TyPiQL Telemetry —
; it has to be listening from the moment a session starts.
LAZY = NONE

[WINDOW_...]
ID = typiql_puppet
NAME = TyPiQL Puppet
ICON = icon.png
FUNCTION_MAIN = windowMain
SIZE = 300, 140
FLAGS = AUTO_RESIZE
